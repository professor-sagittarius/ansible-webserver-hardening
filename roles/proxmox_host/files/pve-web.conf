# /etc/fail2ban/filter.d/pve-web.conf
# fail2ban filter for Proxmox VE web UI authentication failures.
# Used with backend = systemd and journalmatch = _SYSTEMD_UNIT=pveproxy.service

[Definition]
failregex = pveproxy\[.*\]: authentication failure; rhost=<HOST>
ignoreregex =
