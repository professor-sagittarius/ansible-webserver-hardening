# roles/proxmox_host/handlers/main.yml
- name: reload sysctl
  ansible.builtin.command: sysctl --system
  changed_when: true

- name: reload auditd
  ansible.builtin.service:
    name: auditd
    state: restarted
